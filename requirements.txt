# DSA502 — Cardiovascular Disease Project (Phase 1 + Phase 2 data expansion)
# Tested on Python 3.10–3.12

# Core data + ML
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
matplotlib>=3.7
seaborn>=0.12

# Dataset access
kagglehub>=0.2          # Kaggle datasets (sulianova CVD, BRFSS, Framingham, Stroke, Heart Attack)
ucimlrepo>=0.0.7        # UCI Heart Disease + Z-Alizadeh Sani CAD
opendatasets>=0.1.22    # alternative Kaggle downloader fallback

# Phase 2 tree models
xgboost>=2.0            # gradient boosting on combined data

# Notebook runtime
jupyter>=1.0
ipykernel>=6.0
